---
# ===================================================================================================
# INVENTORY PLAYBOOK GENERATOR - KEY SCENARIOS
# ===================================================================================================
#
# This playbook demonstrates key scenarios for generating YAML configurations
# for the inventory_workflow_manager module based on existing device inventory in
# Cisco Catalyst Center.
#
# KEY FEATURES:
# 1. Three Independent Components: device_details, provision_device, interface_details
# 2. Global Filters: ip_address_list applies to ALL components
# 3. Component-Specific Filters: Independent filtering for each component
# 4. Three-Document Output: Separate YAML documents for each component
# 5. HTTP Fields Support: http_username, http_password, http_port, http_secure
# 6. Smart File Creation: No file created if no data matches filters
# 7. Preferred Syntax: file_path and file_mode are top-level module arguments
#
# ===================================================================================================

- name: Cisco Catalyst Center Inventory Playbook Generator - Key Scenarios
  hosts: localhost
  connection: local
  gather_facts: false
  vars_files:
    - "credentials.yml"

  tasks:
    # ===================================================================================
    # SCENARIO 1: Complete Discovery - All Components
    # ===================================================================================
    # Description: Auto-discovers ALL devices and generates all three components
    # Use Case: Initial migration, complete infrastructure backup
    # Output: Single YAML with 3 documents (device details, provision, interfaces)
    # ===================================================================================
    - name: "SCENARIO 1: Complete Discovery - All Components"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_all_devices_complete.yml"
        file_mode: "overwrite"
      tags: [scenario1, complete_discovery]

    # ===================================================================================
    # SCENARIO 2: Specific IPs - All Three Components
    # ===================================================================================
    # Description: Generate all three components for specific IP addresses
    # Use Case: Targeted device migration with full configuration
    # Output: 3 documents for specified IPs
    # ===================================================================================
    - name: "SCENARIO 2: Specific IPs - All Three Components"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_specific_ips.yml"
        file_mode: "overwrite"
        config:
          global_filters:
            ip_address_list:
              - "172.27.248.223"
              - "204.1.2.3"
              - "205.1.2.67"
          component_specific_filters:
            components_list: ["device_details", "provision_device", "interface_details"]
      tags: [scenario2, specific_ips]

    # ===================================================================================
    # SCENARIO 3: Device Details Only
    # ===================================================================================
    # Description: Generate only device details component (credentials and basic info)
    # Use Case: Device inventory only, no provisioning or interface changes
    # Output: Single document with device credentials
    # ===================================================================================
    - name: "SCENARIO 3: Device Details Only"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_device_details_only.yml"
        file_mode: "overwrite"
        config:
          global_filters:
            ip_address_list:
              - "172.27.248.223"
              - "204.1.2.2"
          component_specific_filters:
            components_list: ["device_details"]
      tags: [scenario3, device_details_only]

    # ===================================================================================
    # SCENARIO 4: Provision Device Only - Site Filter
    # ===================================================================================
    # Description: Generate only provision config for devices at a specific site
    # Use Case: Site-specific provisioning without modifying device credentials
    # Output: Single document with provision config
    # ===================================================================================
    - name: "SCENARIO 4: Provision Device Only - Site Filter"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_provision_only.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["provision_device"]
            provision_device:
              site_name: "Global/Site_India/Karnataka/Bangalore/BLD_2/Floor_1"
      tags: [scenario4, provision_only]

    # ===================================================================================
    # SCENARIO 5: Interface Details Only
    # ===================================================================================
    # Description: Generate only interface details for specific devices
    # Use Case: Interface audit, VLAN configuration review
    # Output: Single document with interface configurations
    # ===================================================================================
    - name: "SCENARIO 5: Interface Details Only"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_interface_only.yml"
        file_mode: "overwrite"
        config:
          global_filters:
            ip_address_list:
              - "205.1.2.67"
              - "205.1.2.68"
          component_specific_filters:
            components_list: ["interface_details"]
      tags: [scenario5, interface_only]

    # ===================================================================================
    # SCENARIO 6: Independent Component Filters
    # ===================================================================================
    # Description: Demonstrates INDEPENDENT filtering for different components:
    #              - device_details: Only ACCESS role devices
    #              - provision_device: Only specific site (independent of role filter)
    #              - interface_details: All devices from global filter
    # Use Case: Show that each component filters independently
    # Output: 3 documents with different device sets based on independent filters
    # ===================================================================================
    - name: "SCENARIO 6: Independent Component Filters"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_independent_filters.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["device_details", "provision_device", "interface_details"]
            device_details:
              role: "ACCESS"
            provision_device:
              site_name: "Global/Site_India/Karnataka/Bangalore/BLD_2/Floor_1"
      tags: [scenario6, independent_filters]

    # ===================================================================================
    # SCENARIO 7: Global Filter + Component-Specific Filters
    # ===================================================================================
    # Description: Global IP filter applies to ALL components, then provision filter applied
    # Use Case: Target specific devices, then filter provision by site
    # Output: 3 documents, all using same global IPs, provision filtered by site
    # ===================================================================================
    - name: "SCENARIO 7: Global + Component-Specific Filters"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_global_component_filters.yml"
        file_mode: "overwrite"
        config:
          global_filters:
            ip_address_list:
              - "172.27.248.223"
              - "172.27.248.224"
              - "205.1.2.67"
          component_specific_filters:
            components_list: ["device_details", "provision_device", "interface_details"]
            provision_device:
              site_name: "Global/Site_India/Karnataka/Bangalore/BLD_2/Floor_1"
      tags: [scenario7, global_component]

    # ===================================================================================
    # SCENARIO 8: Role Filter - ACCESS Devices
    # ===================================================================================
    # Description: Generate device details for ACCESS role devices only
    # Use Case: Access layer device inventory
    # Output: Single document with ACCESS role devices
    # ===================================================================================
    - name: "SCENARIO 8: ACCESS Role Devices"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_access_role.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["device_details"]
            device_details:
              role: "ACCESS"
      tags: [scenario8, access_role]

    # ===================================================================================
    # SCENARIO 9: Multiple Roles - OR Logic
    # ===================================================================================
    # Description: Generate device details for multiple roles (ACCESS OR BORDER ROUTER)
    # Use Case: Combined access and border device inventory
    # Output: Single document with devices matching ANY of the specified roles
    # ===================================================================================
    - name: "SCENARIO 9: Multiple Roles - OR Logic"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_multi_role.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["device_details"]
            device_details:
              role: ["ACCESS", "BORDER ROUTER", "CORE"]
      tags: [scenario9, multi_role]

    # ===================================================================================
    # SCENARIO 10: Device Details + Provision
    # ===================================================================================
    # Description: Generate device details and provision, skip interface details
    # Use Case: Device onboarding without interface changes
    # Output: 2 documents - device details and provision
    # ===================================================================================
    - name: "SCENARIO 10: Device Details + Provision"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_device_provision.yml"
        file_mode: "overwrite"
        config:
          global_filters:
            ip_address_list:
              - "172.27.248.223"
          component_specific_filters:
            components_list: ["device_details", "provision_device"]
      tags: [scenario10, device_provision]

    # ===================================================================================
    # SCENARIO 11: Multiple Sites - Independent Files
    # ===================================================================================
    # Description: Generate multiple files for different sites
    # Use Case: Multi-site deployment with site-specific configs
    # Output: Multiple YAML files, each with different site provision configs
    # ===================================================================================
    # Site 1: Bangalore BLD_1
    - name: "SCENARIO 11a: Multiple Sites - Site 1 Bangalore BLD_1"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_site_bangalore_bld1.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["provision_device"]
            provision_device:
              site_name: "Global/Site_India/Karnataka/Bangalore/BLD_1/Floor_1"
      tags: [scenario11, multi_site]

    # Site 2: Bangalore BLD_2
    - name: "SCENARIO 11b: Multiple Sites - Site 2 Bangalore BLD_2"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_site_bangalore_bld2.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["provision_device"]
            provision_device:
              site_name: "Global/Site_India/Karnataka/Bangalore/BLD_2/Floor_1"
      tags: [scenario11, multi_site]

    # ===================================================================================
    # SCENARIO 12: Default File Path - Auto-Generated Name
    # ===================================================================================
    # Description: No file_path specified - generates with timestamp
    # Use Case: Quick discovery without worrying about naming
    # Output: File named inventory_workflow_manager_playbook_<timestamp>.yml
    # ===================================================================================
    - name: "SCENARIO 12: Default File Path (Auto-Generated)"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        config:
          global_filters:
            ip_address_list:
              - "172.27.248.223"
          component_specific_filters:
            components_list: ["device_details"]
      tags: [scenario12, default_path]

    # ===================================================================================
    # SCENARIO 13: Interface Details with Single Interface Name Filter
    # ===================================================================================
    # Description: Filter interface_details to include only specific interface names
    # Use Case: Focus on specific VLAN or Loopback interface configuration
    # Output: Single document with only specified interface names (e.g., Vlan100)
    # ===================================================================================
    - name: "SCENARIO 13: Interface Details - Single Interface Filter (Vlan100)"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_interface_vlan100_only.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["interface_details"]
            interface_details:
              interface_name: ["Vlan100"]
      tags: [scenario13, interface_single_filter]

    # ===================================================================================
    # SCENARIO 14: Interface Details with Multiple Interface Name Filters
    # ===================================================================================
    # Description: Filter interface_details to include multiple specific interface names
    # Use Case: Audit and configure multiple critical interfaces across all devices
    # Output: Single document with only specified interface names (Vlan100, Loopback0, etc.)
    # ===================================================================================
    - name: "SCENARIO 14: Interface Details - Multiple Interface Filters"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_interface_multi_filter.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["interface_details"]
            interface_details:
              interface_name: ["Vlan100", "Vlan111", "Loopback0"]
      tags: [scenario14, interface_multi_filter]

    # ===================================================================================
    # SCENARIO 15: Global IP Filter + Interface Name Filter
    # ===================================================================================
    # Description: Combine global IP filter with specific interface name filter
    # Use Case: Get specific interfaces only from targeted devices
    # Output: Single document with only specified IPs and specified interfaces
    # Note: If device doesn't have the specified interface, no config generated for it
    # ===================================================================================
    - name: "SCENARIO 15: Global IP + Interface Name Filter"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_ip_interface_filter.yml"
        file_mode: "overwrite"
        config:
          global_filters:
            ip_address_list:
              - "100.1.1.61"
              - "172.27.248.223"
              - "205.1.2.67"
          component_specific_filters:
            components_list: ["interface_details"]
            interface_details:
              interface_name: ["Vlan100", "Loopback0", "GigabitEthernet0/0"]
      tags: [scenario15, ip_interface_filter]

    # ===================================================================================
    # SCENARIO 16: Device Details + Interface Details with Interface Filter
    # ===================================================================================
    # Description: Generate device credentials and only specific interfaces
    # Use Case: Onboard devices and configure specific interfaces simultaneously
    # Output: 2 documents - device details and filtered interface configs
    # ===================================================================================
    - name: "SCENARIO 16: Device Details + Filtered Interfaces"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_device_filtered_interfaces.yml"
        file_mode: "overwrite"
        config:
          global_filters:
            ip_address_list:
              - "172.27.248.223"
              - "205.1.2.67"
          component_specific_filters:
            components_list: ["device_details", "interface_details"]
            interface_details:
              interface_name: ["Loopback0", "Vlan100"]
      tags: [scenario16, device_filtered_interface]

    # ===================================================================================
    # SCENARIO 17: All Components with Interface Filter
    # ===================================================================================
    # Description: Generate all three components with interface_details filtered by name
    # Use Case: Complete infrastructure migration with controlled interface updates
    # Output: 3 documents - device details, provision config, and filtered interfaces
    # ===================================================================================
    - name: "SCENARIO 17: All Components - Device + Provision + Filtered Interfaces"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_all_filtered_interfaces.yml"
        file_mode: "overwrite"
        config:
          global_filters:
            ip_address_list:
              - "172.27.248.223"
              - "205.1.2.67"
          component_specific_filters:
            components_list: ["device_details", "provision_device", "interface_details"]
            provision_device:
              site_name: "Global/Site_India/Karnataka/Bangalore/BLD_2/Floor_1"
            interface_details:
              interface_name: ["Loopback0"]
      tags: [scenario17, all_components_filtered]

    # ===================================================================================
    # SCENARIO 18: Interface Filter with No Matching Interfaces
    # ===================================================================================
    # Description: Filter interfaces that may not exist on all devices
    # Use Case: Handle scenarios where some devices don't have specified interfaces
    # Output: Only generates configs for devices that have the specified interfaces
    # Note: No file created if no devices have matching interfaces
    # ===================================================================================
    - name: "SCENARIO 18: Interface Filter - No Match Handling"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_interface_no_match.yml"
        file_mode: "overwrite"
        config:
          global_filters:
            ip_address_list:
              - "172.27.248.223"
              - "172.27.248.224"
              - "172.27.248.82"
          component_specific_filters:
            components_list: ["interface_details"]
            interface_details:
              interface_name: ["TenGigabitEthernet1/1/1"]  # Might not exist on all devices
      tags: [scenario18, interface_no_match]

    # ===================================================================================
    # SCENARIO 19: GigabitEthernet Interface Filter
    # ===================================================================================
    # Description: Filter for physical GigabitEthernet interfaces only
    # Use Case: Configure access ports or uplinks specifically
    # Output: Single document with GigabitEthernet interface configs only
    # ===================================================================================
    - name: "SCENARIO 19: GigabitEthernet Interfaces Only"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_gigabitethernet_only.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["interface_details"]
            interface_details:
              interface_name: ["GigabitEthernet0/0", "GigabitEthernet1/0/11", "GigabitEthernet1/0/12"]
      tags: [scenario19, gigabitethernet_only]

    # ===================================================================================
    # SCENARIO 20: Role-Based Devices with Interface Filter
    # ===================================================================================
    # Description: Get ACCESS role devices and filter their specific interfaces
    # Use Case: Configure access layer devices with specific interface updates
    # Output: 2 documents - filtered device details (ACCESS only) and their interfaces
    # ===================================================================================
    - name: "SCENARIO 20: ACCESS Devices with Specific Interface Filter"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_access_devices_interface_filter.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["device_details", "interface_details"]
            device_details:
              role: "ACCESS"
            interface_details:
              interface_name: ["GigabitEthernet0/0", "GigabitEthernet0/1"]
      tags: [scenario20, access_devices_interface]
    # ===================================================================================
    # SCENARIO 21: User Defined Fields Only
    # ===================================================================================
    # Description: Generate only user_defined_fields configuration for all devices
    # Use Case: Inventory audit of custom UDF values assigned to devices
    # Output: Single document with consolidated UDF entries grouped by identical UDF sets
    # ===================================================================================
    - name: "SCENARIO 21: User Defined Fields Only"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_udf_only.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["user_defined_fields"]
      tags: [scenario21, udf_only]

    # ===================================================================================
    # SCENARIO 22: All Components Including User Defined Fields
    # ===================================================================================
    # Description: Generate all four components: device_details, provision_device,
    #              interface_details, and user_defined_fields
    # Use Case: Complete infrastructure migration with all metadata and custom fields
    # Output: 4 YAML documents with all configurations
    # ===================================================================================
    - name: "SCENARIO 22: All Components Including User Defined Fields"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_all_components_with_udf.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["device_details", "provision_device", "interface_details", "user_defined_fields"]
      tags: [scenario22, all_components_udf]

    # ===================================================================================
    # SCENARIO 23: Device Details + User Defined Fields
    # ===================================================================================
    # Description: Generate device credentials and their user-defined field assignments
    # Use Case: Device onboarding with custom metadata preservation
    # Output: 2 documents - device details and UDF configurations
    # ===================================================================================
    - name: "SCENARIO 23: Device Details + User Defined Fields"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_device_udf.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["device_details", "user_defined_fields"]
      tags: [scenario23, device_udf]

    # ===================================================================================
    # SCENARIO 24: Global IP Filter + User Defined Fields
    # ===================================================================================
    # Description: Generate UDF configurations only for devices at specific IP addresses
    # Use Case: Audit custom fields for specific set of devices
    # Output: UDF entries for devices matching the IP filter
    # Note: Replace IPs with actual device management IPs from your environment
    # ===================================================================================
    - name: "SCENARIO 24: Global IP Filter + User Defined Fields"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_ip_filter_udf.yml"
        file_mode: "overwrite"
        config:
          global_filters:
            ip_address_list:
              - "206.1.2.3"
              - "206.1.2.4"
              - "172.27.248.223"
          component_specific_filters:
            components_list: ["user_defined_fields"]
      tags: [scenario24, ip_filter_udf]

    # ===================================================================================
    # SCENARIO 25: Provision Device + User Defined Fields (Site-Specific)
    # ===================================================================================
    # Description: Generate provisioning config for specific site and UDF for all devices
    # Use Case: Site provisioning with device metadata tracking
    # Output: 2 documents - provision_device (site-filtered) and user_defined_fields (all)
    # ===================================================================================
    - name: "SCENARIO 25: Provision Device Site Filter + UDF"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_provision_site_udf.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["provision_device", "user_defined_fields"]
            provision_device:
              site_name: "Global/Site_India/Karnataka/Bangalore/BLD_2/Floor_1"
      tags: [scenario25, provision_site_udf]

    # ===================================================================================
    # SCENARIO 26: Interface Details + User Defined Fields
    # ===================================================================================
    # Description: Generate interface configurations and device custom field assignments
    # Use Case: Interface updates with device metadata synchronization
    # Output: 2 documents - interface_details (filtered or all) and user_defined_fields
    # ===================================================================================
    - name: "SCENARIO 26: Interface Details + User Defined Fields"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_interface_udf.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["interface_details", "user_defined_fields"]
      tags: [scenario26, interface_udf]

    # ===================================================================================
    # SCENARIO 27: Interface Filter + UDF (Specific Interfaces Only)
    # ===================================================================================
    # Description: Generate UDF and only specific interface names
    # Use Case: Configure critical interfaces and sync custom device metadata
    # Output: 2 documents - filtered interface_details and user_defined_fields
    # ===================================================================================
    - name: "SCENARIO 27: Specific Interfaces + User Defined Fields"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_interface_name_udf.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["interface_details", "user_defined_fields"]
            interface_details:
              interface_name: ["Loopback0", "Vlan100"]
      tags: [scenario27, interface_name_udf]

    # ===================================================================================
    # SCENARIO 28: Role-Based Device Details + UDF
    # ===================================================================================
    # Description: Generate UDF for devices of specific roles (e.g., ACCESS layer)
    # Use Case: Access layer device migration with preserved custom metadata
    # Output: 2 documents - role-filtered device_details and corresponding user_defined_fields
    # ===================================================================================
    - name: "SCENARIO 28: Role-Based Device Details + UDF"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_access_role_udf.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["device_details", "user_defined_fields"]
            device_details:
              role: "ACCESS"
      tags: [scenario28, role_based_udf]

    # ===================================================================================
    # SCENARIO 29: Complex Multi-Filter with UDF
    # ===================================================================================
    # Description: Multiple global filters with device roles and provision site + UDF
    # Use Case: Multi-site, multi-role infrastructure with comprehensive metadata
    # Output: 4 documents - filtered device_details, provision_device, interfaces, and UDF
    # ===================================================================================
    - name: "SCENARIO 29: Multi-Filter Complex Scenario with UDF"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_complex_multi_filter_udf.yml"
        file_mode: "overwrite"
        config:
          global_filters:
            ip_address_list:
              - "172.27.248.223"
              - "172.27.248.224"
              - "205.1.2.67"
          component_specific_filters:
            components_list: ["device_details", "provision_device", "interface_details", "user_defined_fields"]
            device_details:
              role: "ACCESS"
            provision_device:
              site_name: "Global/Site_India/Karnataka/Bangalore"
            interface_details:
              interface_name: ["Loopback0", "Vlan100"]
      tags: [scenario29, complex_multi_filter_udf]

    # ===================================================================================
    # SCENARIO 30: UDF Audit - All Devices with Custom Metadata
    # ===================================================================================
    # Description: Comprehensive UDF audit showing all devices and their custom field values
    # Use Case: Complete inventory audit with custom metadata for compliance reporting
    # Output: Consolidated UDF configurations grouped by identical field sets
    # Note: Uses generate_all_configurations for maximum device coverage
    # ===================================================================================
    - name: "SCENARIO 30: Complete UDF Audit - All Devices"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_udf_audit_complete.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["user_defined_fields"]
      tags: [scenario30, udf_audit]

    # ===================================================================================
    # SCENARIO 31: UDF Name Filter - Specific Field Names Only
    # ===================================================================================
    # Description: Generate UDF configurations but only for specified UDF field names
    # Use Case: Focus on specific custom fields (e.g., only "Cisco Switches" and "To_test_udf")
    # Output: UDF entries grouped by identical field sets (only specified names)
    # ===================================================================================
    - name: "SCENARIO 31: UDF Name Filter - Specific Field Names"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_udf_name_filter.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["user_defined_fields"]
            user_defined_fields:
              name: ["Cisco Switches", "To_test_udf"]
      tags: [scenario31, udf_name_filter]

    # ===================================================================================
    # SCENARIO 32: UDF Value Filter - Specific Field Values Only
    # ===================================================================================
    # Description: Generate UDF configurations but only for devices matching specific values
    # Use Case: Audit devices with any of the specified values (OR logic)
    # Output: UDF entries only where values match any in the list
    # Note: Matches devices with values: 2234, value123, or value12345
    # ===================================================================================
    - name: "SCENARIO 32: UDF Value Filter - Specific Field Values"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_udf_value_filter.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["user_defined_fields"]
            user_defined_fields:
              value: ["2234", "value123", "value12345"]
      tags: [scenario32, udf_value_filter]

    # ===================================================================================
    # SCENARIO 33: Global IP Filter + UDF Name Filter
    # ===================================================================================
    # Description: Generate UDF for specific IPs but only for specified UDF field names
    # Use Case: Audit custom fields for targeted devices
    # Output: UDF entries for devices matching IP filter and field names
    # ===================================================================================
    - name: "SCENARIO 33: Global IP Filter + UDF Name Filter"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_ip_udf_name_filter.yml"
        file_mode: "overwrite"
        config:
          global_filters:
            ip_address_list:
              - "206.1.2.4"
              - "204.1.2.2"
              - "204.1.2.3"
          component_specific_filters:
            components_list: ["user_defined_fields"]
            user_defined_fields:
              name: ["Cisco Switches", "Test123"]
      tags: [scenario33, ip_udf_name_filter]

    # ===================================================================================
    # SCENARIO 34: Device Details + Filtered UDF Names
    # ===================================================================================
    # Description: Generate device credentials with only specific UDF field names
    # Use Case: Device migration preserving only critical custom metadata
    # Output: 2 documents - device_details and UDF (name-filtered)
    # ===================================================================================
    - name: "SCENARIO 34: Device Details + Filtered UDF Names"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_device_udf_name_filter.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["device_details", "user_defined_fields"]
            user_defined_fields:
              name: ["Cisco Switches", "Test321"]
      tags: [scenario34, device_udf_name_filter]

    # ===================================================================================
    # SCENARIO 35: All Components + UDF Name and Value Filters
    # ===================================================================================
    # Description: Complete migration with UDF filtered by both name and value
    # Use Case: Multi-site deployment with specific metadata filtering
    # Output: Multiple documents with filtered UDF entries
    # Note: Filters by field names AND by specific values (combination filter)
    # ===================================================================================
    - name: "SCENARIO 35: All Components + UDF Name & Value Filters"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_all_udf_filtered.yml"
        file_mode: "overwrite"
        config:
          global_filters:
            ip_address_list:
              - "206.1.2.4"
              - "204.1.2.2"
              - "204.1.2.3"
          component_specific_filters:
            components_list: ["device_details", "user_defined_fields"]
            user_defined_fields:
              name: ["Cisco Switches", "Test123", "Test321"]
              value: ["2234", "value321"]
      tags: [scenario35, all_components_udf_filtered]

    # ===================================================================================
    # SCENARIO 36: UDF Name Filter - Single String
    # ===================================================================================
    # Description: Generate UDF using single name string (no list)
    # Use Case: Validate string input support for user_defined_fields.name
    # Output: UDF entries containing only "Cisco Switches"
    # ===================================================================================
    - name: "SCENARIO 36: UDF Name Filter - Single String"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_udf_name_filter_single.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["user_defined_fields"]
            user_defined_fields:
              name: "Cisco Switches"
      tags: [scenario36, udf_name_filter_str]

    # ===================================================================================
    # SCENARIO 37: UDF Value Filter - Single String
    # ===================================================================================
    # Description: Generate UDF using single value string (no list)
    # Use Case: Validate string input support for user_defined_fields.value
    # Output: UDF entries where value equals "value12345"
    # ===================================================================================
    - name: "SCENARIO 37: UDF Value Filter - Single String"
      cisco.dnac.inventory_playbook_config_generator:
        dnac_host: "{{ dnac_host }}"
        dnac_username: "{{ dnac_username }}"
        dnac_password: "{{ dnac_password }}"
        dnac_verify: "{{ dnac_verify }}"
        dnac_port: "{{ dnac_port }}"
        dnac_version: "{{ dnac_version }}"
        dnac_debug: "{{ dnac_debug }}"
        dnac_log: true
        dnac_log_level: INFO
        state: gathered
        file_path: "inventory_udf_value_filter_single.yml"
        file_mode: "overwrite"
        config:
          component_specific_filters:
            components_list: ["user_defined_fields"]
            user_defined_fields:
              value: "value12345"
      tags: [scenario37, udf_value_filter_str]
