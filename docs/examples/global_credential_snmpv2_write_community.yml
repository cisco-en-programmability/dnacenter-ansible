- hosts: localhost
  gather_facts: no
  tasks:
  - name: Get/create/delete global credential snmpv2 write community
    cisco.dnac.global_credential_snmpv2_write_community:
      dnac_host: 192.168.196.2
      dnac_port: 443
      dnac_username: altus
      dnac_password: Altus123
      dnac_version: 2.1.1
      dnac_verify: False
      #
      state: create
      payload:
      - writeCommunity: SomeCommunityString
        description: SomeDescription
      #
      # state: update
      # writeCommunity: SomeCommunityString
      # description: SomeDescription
      # 
    register: result

  - name: Show retrieved object
    debug:
      msg: "{{ result }}"     