version: 2.1


jobs:
  build:
    
    parameters: 
      ansible_cisco_dnac_version:
        type: string
        default: "6.9.0"

    machine: true
    resource_class: rukapse/dnacenter-ansible
    
    steps:
      - run:
          name: Clone Git repository
          command: git clone --depth=1 https://github.com/rukapse/dnacenter-ansible.git

workflows:
  testing:
    jobs:
      - build