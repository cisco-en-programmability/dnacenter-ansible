version: 2.1


jobs:
  build:
    
    parameters: 
      ansible_cisco_dnac_version:
        type: string
        default: "6.9.0"

    machine: true
    resource_class: rukapse/dnacenter-ansible
    
    steps:
      - run:
          name: Confirm SSH keys
          command: |
            ssh-add -l
            ls -l ~/.ssh
      - add_ssh_keys:
          fingerprints:
            - "SHA256:J0xQGHm/j7txCuHdPam0+Ty+9VpEELCMJo8eYptUYO0"
      - checkout 

workflows:
  testing:
    jobs:
      - build