version: 2.1


jobs:
  build:
    
    parameters: 
      ansible_cisco_dnac_version:
        type: string
        default: "6.9.0"

    machine: true
    resource_class: rukapse/dnacenter-ansible
    
    steps:
      - checkout:
          git:
            url: https://github.com/rukapse/dnacenter-ansible.git
          submodules: false  # Disable submodules, as they may still use SSH
          depth: 1  
workflows:
  testing:
    jobs:
      - build