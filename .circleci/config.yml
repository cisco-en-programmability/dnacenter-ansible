version: 2.1


jobs:
  build:
    
    parameters: 
      ansible_cisco_dnac_version:
        type: string
        default: "6.9.0"

    machine: true
    resource_class: rukapse/dnacenter-ansible
    
    steps:
      - run:
          name: Confirm SSH keys
          command: |
            ssh-add -l
            ls -l ~/.ssh
      - checkout 

workflows:
  testing:
    jobs:
      - build